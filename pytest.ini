[pytest]
# Demo/manual runners are named manual_check_*.py (not test_*.py) so they are
# not collected. ppe_reference holds the downloaded model, never test code.
norecursedirs = ppe_reference .venv venv build dist *.egg-info .git
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
