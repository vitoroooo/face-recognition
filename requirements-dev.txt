# Development & testing dependencies
# Test suite for the ppe_compliance package + camera utils runs without the
# heavy CV stack (dlib/face_recognition/torch), so use the headless OpenCV.
opencv-python-headless>=4.8.0
numpy>=1.24.0
pytest>=7.4.0
ruff>=0.8.0
pre-commit>=3.5.0
